# Assets

Place the WordTree logo image here:

```text
assets/wordtree-logo.png
```

The sidebar brand mark will automatically use this image. If the image is missing, the app falls back to the built-in `W` mark.
